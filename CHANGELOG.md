# Changelog

All notable changes to this project are documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and the project uses [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- The report now says which rows depend on who is signed in. Reusing the person's own signed-in browser is the design -- it is how a source is read without simulating a login -- and the cost is that the list it returns is not the list anyone else would get: a live round came back from irishjobs.ie with `searchOrigin=membersarea`, and LinkedIn and Indeed were signed in too. Unsaid, two runs that saw different lists for that reason look like a change in the market. Every browser task now carries `reproducibility: session_dependent`, a row found through the browser route carries `session_dependent`, the card and the detail pane label it, and the header counts them: "three of these came from your signed-in browser" is the sentence that stops a shorter list next week from reading as fewer jobs. One such route among several is enough, since the question is whether this row's visibility depended on the session. An ATS API and a Web Search return the same thing to anyone and are not marked -- marking those too would make the label mean nothing.

- A source whose listings live on another host can now say so, and publicjobs.ie does. It serves its own front page and serves every vacancy from `publicjobs.tal.net`, so the browser boundary -- derived from the entry URL's host -- refused the only page with jobs on it, and the live round on 2026-09-25 failed that task with `host_boundary`. `listing_hosts` on the seed joins the task's `allowed_hosts`, so the widening is recorded in the catalog and visible in the plan rather than decided by whoever is browsing; a host nobody declared is still out of bounds, and the ATS handoff list still widens what a redirect means rather than what may be browsed. Validated as a bare lowercase hostname, because anything looser would let a seed open a whole URL space by writing a path into it, and a host that merely repeats the entry host is refused as a widening that widens nothing.

- A site that writes down its own pace is now heard. `check_robots.py` reports `Crawl-delay`, seeds carry `min_interval_ms`, and `browser_control.py` reads the catalog and uses whichever is slower -- the global floor or what the source asked for. `publicjobs.tal.net` publishes `Crawl-delay: 10`, twice the five-second floor, so every read of it until now was at double the rate it requested in writing. Read from the catalog rather than passed in by the caller, because a pace the executor has to remember to apply is one that gets forgotten on the run where it matters; a catalog that will not load falls back to the floor rather than stopping the action being recorded. Only ever slower: a seed cannot ask to be read faster than everything else. A site that says nothing about pace returns `None`, not a default, since an invented delay is indistinguishable from one the site actually asked for.

- That host also closes everything to `ClaudeBot` and `GPTBot` by name while leaving `*` open. Reading the named group as ours would disable a public-sector portal nobody closed to us, and reading ours as theirs would be the same mistake pointed the other way -- so the `*` group governs, and the operator's stance is recorded on the seed as `operator_blocks_named_ai_crawlers` for a person to weigh rather than left unsaid.

- Amazon's Irish listings are fetched as JSON instead of browsed. `search.json` filtered by country and asked for no query behaves exactly like a board: measured 2026-09-26, three requests and 1.5 seconds returned all 208 Irish postings, every one carrying its full description, 32 fields each, nothing dropped. The browser route reads the same postings a page of twenty-five at a time with a five-second interval between actions. No `base_query` is sent -- a board is fetched whole and filtered locally, which is what makes its result reproducible and its query-writing unnecessary, and sending search terms to this one endpoint would give it a different and unrepeatable basis from every other source; the country code is which listing to read, not a search term. `amazon-careers` keeps `web_search` and `manual_browser` and gives up `public_read_only_page`, so the same employer is not also browsed.

- `public_read_only_endpoint` stopped being an access method the catalog accepted and no seed could use. `ats_view_from_registry` selected on `source_type == "ats_board"`, so the structured channel only ever meant ATS boards however a seed described itself; it now selects on having a fetchable provider and a board token. `ATS_API_PROVIDERS` and `STRUCTURED_PROVIDERS` are now two sets rather than one, because they answer two questions -- being fetchable is not a claim to have an ATS API, and collapsing them would let a seed declare `ats_public_api` for a provider that has none. Writing fetch health back no longer rebuilds a whole record from the board view either: that would have relabelled a seeded `company_careers` endpoint as an `ats_board` with access methods it does not have, so only the health fields move and a new record is built only for a newly discovered board, which is always one of the three ATS providers.

- `check_robots.py` reads a site's `robots.txt` and says whether a given URL is permitted, which nothing here could do before -- the guardrail said to respect robots and there was no way to ask. It is not `urllib.robotparser`: the standard library returns the first matching rule in file order, while RFC 9309 section 2.2.2 says the longest matching path wins with a tie going to Allow, and the difference decides real cases. Microsoft's careers host publishes `Disallow: /` followed by `Allow: /careers`; the standard library reads that as everything forbidden, which would have had this catalog refuse a source its operator deliberately opened. A test pins the disagreement so nobody swaps in the shorter call.

- Ten `company_careers` seeds carried `verified: true`, `verification_method: official_global_job_search` and `automation_allowed: true` with nothing behind them: only `amazon.jobs` had ever been looked at, and the catalog was asserting a verification nobody had performed. All ten were checked on 2026-09-26 -- entry URL and `robots.txt`, one request per host, five seconds apart -- and two turned out not to be what the catalog said. Accenture's file allows `/careers/jobsearch` and forbids `/careers/jobsearch?`, so every search is refused and only the landing page is not; `automation_allowed` is now `false` and `public_read_only_page` is gone, while `web_search` and `manual_browser` stay, since a person opening the page themselves is not what robots addresses. Google forbids `?page=` on the jobs results path, so the first page is readable and the pagination the browser workflow does by default is not; recorded as `robots_disallow_result_pagination`, which `discovery_plan` copies onto every browser task built from that seed. Checking only each entry URL would have called all ten usable -- the query string is where both refusals live. The retired `official_global_job_search` value is pinned out by a test so it cannot be copied into the next seed, and a second test makes the recorded refusal enforce itself: a seed carrying `robots_disallow_search_queries` cannot also claim automation or a direct access method. Measurements are in `docs/company-careers-robots-2026-09-26.md`.

- One role advertised more than once is now labelled as such. MongoDB opened two Greenhouse requisitions for the same job on 2026-09-25 -- 41 lines of description apart by eight characters, one saying "Ireland" and the other "Cork, Ireland; Dublin, Ireland" -- and the report showed it twice with nothing connecting the two, so one Top-N slot went to a posting the reader had already considered. They are not merged, and should not be: each carries its own `gh_jid` and its own apply URL, so dropping one would drop a real way in. Rows that share a company and an exactly matching title now carry `same_role`, the list card shows a chip, and the detail pane lists the other postings' locations and links. The grouping reads the company plus the title as written, normalized only for whitespace and case -- deliberately not the row's `dedup_key`, which is a weak key built for merging alongside a location and identity check: `normalize_title` strips parentheticals and anything after a dash, so Intercom's "Senior Data Scientist - AI Tooling", "- Growth" and "(GTM)" all collapse onto one key, and those are three different jobs. Checked against the live table: 4 of 30 rows group, and reusing the merge key would have grouped 9.

### Fixed

- `config.json` went on setting `failed_events_max: 0` after the threshold it names was removed. The thresholds in force are the defaults with `monitoring_thresholds` merged over them, and that merged mapping is published as `thresholds` in the health report -- so a key no `_breach` call reads is still advertised there as a limit being applied, and this one outlived the switch to `failed_event_rate_max` doing exactly that. The key is gone. It hid under the test that catches config knobs nothing reads, which exempted `monitoring_thresholds` as a whole; the exemption stays, because the block really is consumed by nested key rather than by its own name, but each key inside it is now checked against `DEFAULT_THRESHOLDS`, so a threshold the code does not enforce fails CI instead of being reported as one that does.

- The test suite was writing to this installation's own data, and most of what the monitoring reported was its own tests. On 2026-09-26 the live metrics store held 1,324 rows of which 820 carried a test run id -- 62% -- including 57 false `browser_paced_too_fast` events written by a single afternoon's runs. Seven tests reached the live store by omission rather than intent, because the code under test had no way to be pointed elsewhere. Every module's writable path is now redirected to the test's own directory by default, `merge_jobs.py` takes `--metrics-path` and `--table-path` so the merge subprocess can be redirected too -- the two files it writes are the two a caller has to be able to point elsewhere, and a child cannot inherit a monkeypatched module global, and an autouse guard fails the individual test that touches `metrics.jsonl`, `jobs_table.json`, `source_registry.json`, the pacing file, the round budget or the ATS sync state. The 820 polluted rows were removed from the live store; the remaining 504 are real.

- `failed_events_max: 0` was a light that never went off. Over a seven-day window, one failure anywhere turned the health status red and kept it red for the week, so the signal was permanently on and told nobody anything. It is now `failed_event_rate_max`, 2% -- the same line `conflict_rate_max` already draws -- with the raw count still reported beside it, because that is the number a person wants to see. `write_failures_max`, `malformed_events_max` and `malformed_manifests_max` stay at zero: a dropped write, a malformed event and a malformed manifest are not operations that failed but the record itself being wrong, and one of those is already one too many. After the prune the real rate is 15 failures in 479 operations, 3.1%, which still breaches -- correctly, and now by a number that can come back down.

- `browser_control.py` had no way to be tried without writing to the live stores. Every invocation resolved its metrics file, round budget and source pacing state from the skill's own `data/`, so a single rehearsal call landed in the production metrics store and the production pacing state, where it is indistinguishable afterwards from a real round -- this session put a false `browser_paced_too_fast` event into the live metrics exactly that way, and had to hand-restore a pacing timestamp a test had overwritten. `--data-dir` moves all three together; redirecting only the metrics would have left the other two writing live, which is the same failure one notice quieter. Two of the three paths were also bound as default arguments and therefore frozen at import, the defect already found and fixed in `SourcePace`: `BrowserRoundBudget` and `BrowserController` now resolve theirs on construction, and one parametrised test pins all three rather than the one that happened to be caught. A test also covers the read side -- a rehearsal asked how long to wait answers from its own sandbox, not from what the live round touched.

- A browser action whose moment was never captured had no way to say so. `--occurred-at-ms` accepted an exact time or nothing, and nothing meant now -- so one untimed action in a batch reported afterwards claimed to have happened after the actions that really came later, and pacing judged the whole batch on a timeline that never existed. It produced a false `browser_paced_too_fast` in a live round on 2026-09-25, from an ordering mistake that the caller had no correct way to avoid. `--timing unavailable` is the missing third answer, the same shape `timing` already had for a Web Search page run without a clock around the call: the action happened, it was not timed, and nothing is invented to fill the gap. Such an action is not paced and -- equally important -- does not move the source's stored timestamp, because recording a guess would make every later action be measured from a moment nobody observed. It still counts as browser coverage: the page really was fetched, only the stopwatch is missing. It cannot be combined with `--occurred-at-ms`, since half a stopwatch is not a measurement and a caller should not be able to pass a number and disclaim it in the same breath. Because an untimed action is exempt from pacing, `metrics.browsers.untimed` counts them, for the reason `search_pages_untimed` exists: an exemption nobody can see is an exemption nobody audits.

- A browser route that stopped counted as a browser route that ran. `assess_run_completeness` took every event with `ok=True` as evidence the operation happened, and `user_action_required`, `rate_limited` and `resumed` are all written with `ok=True` -- correctly, because stopping at a login wall or backing off a 429 is the route behaving well, not a fault. The effect was that a round whose every browser task hit a login wall reported `complete: True`, and WORKFLOW's promise that a failure report cannot make a broken route look instrumented held only for the routes that broke loudly. Panel state reports had the same hole: `record_state` writes `action=state` to drive the local browser panel, and announcing a state satisfied the gate. Completeness now counts an event only when it reports work -- not a failure, not a lifecycle status, not a state report -- while those events keep their `ok` and stay out of the failure counts, because none of them is a malfunction. `resumed` is in the lifecycle set because it comes only from `HandoffWindow.status_at`, whose one other answer, `timeout`, was already a failure: it says the wait ended, not that anything was fetched. A test pins the full action-status vocabulary against the two sets, so a newly added status has to be placed on one side rather than silently counting as work.

- Pacing counted actions while the site counted requests, and the two differ by an order of magnitude. `browser_min_source_interval_ms` spaces one action from the next, but one card click on an Indeed result page was measured at 14 requests, so five seconds between actions is about 2.8 requests a second rather than the 0.2 the setting reads like -- and nothing anywhere enforced the number a site's own rate limiter actually watches. The request count is now a quantity in its own right: `action --requests N` and `pace --requests N` take what the caller measured in the page (the difference between two same-origin `performance.getEntriesByType('resource')` counts), `SourcePace` keeps a trailing 60-second window of it per source, and `browser_max_requests_per_minute` (default 120, lowerable only) is the ceiling. An action reported without a count is charged `browser_assumed_requests_per_action` (default 10, raisable only), because one is the only count that is certainly wrong. The three defaults are set so the two limits bind at the same moment -- 120 / 10 is 12 actions a minute, which is one every 5000ms -- so an existing caller is paced exactly as before and measuring can only make the answer more accurate: a cheap action stops paying for a page load it never made, and an expensive one stops being free. Going over is written as `failure_kind=browser_request_budget_exceeded` and kept out of the round's completeness, named apart from `browser_paced_too_fast` because every action that trips it kept its interval. `pace` answers with whichever limit binds later, so waiting the advised time is still always sufficient. The stored pace file gains a window alongside the timestamp and still reads the old bare-timestamp form; the window starts empty after the upgrade, which undercounts for at most one minute and invents nothing.

- Link verification read every HTTP error as a closed posting, so a site that refuses bots looked exactly like a site whose job had been taken down. `if code >= 400: alive = False` grouped 403, 429 and 5xx with 404 and 410, while the script's own docstring reserved `null` for the undetermined case. The two mistakes do not cost the same: WORKFLOW removes a job that verifies dead, so reading a refusal as a closure deletes a real posting, whereas reading it as undetermined only forgoes one piece of evidence. It also fell hardest on exactly the wrong channel -- the browser route exists for sites that refuse a plain HTTP client, so its output is the most likely to be refused and the most likely to be thrown away. Caught while verifying a finished round on 2026-09-25: irishjobs.ie answered this script's user agent with 403 and the posting was reported dead, while the same URL opened in a browser with its title intact and no closure wording anywhere. Only 404 and 410 now mean gone; every other status at or above 400 returns `null`, and confirming one of those is the browser layer's job. A test pins the gone-code set so widening it has to be deliberate. The script previously had no tests at all; it now has fourteen, covering both codes that remove a job, six that must not, body-keyword closure, a redirect that drops the job id, transport failures, and the CLI continuing past one bad URL.

- Pacing waited before actions that never reach the site, which was almost the whole cost of reading a page. `read` and `snapshot` operate on the already-loaded document, and the interval was applied to them exactly as to a request. Measured on a live result page on 2026-09-25 by counting the origin's entries in `performance.getEntriesByType('resource')`: four observations back to back issued 0 requests in 102ms, an idle page issued 0 over four seconds, and one card click issued 14. Pacing an observation therefore bought the site nothing and cost a full interval each time -- across a 25-job page, 95% of the elapsed time was this wait. `create`, `navigate`, `act` and `extract` are paced; `read`, `snapshot`, `wait` and `close` are not, and just as importantly they no longer advance the stored timestamp, which would have made the next real request wait out an interval measured from something that sent nothing. `extract` is paced with the network actions because its name does not say where its data comes from, and pacing something local is only slow while not pacing something remote is rude. `pace --source-id S --action A` answers for a specific action and returns zero for an observation, and the result of `record_action` now says whether pacing applied at all. A test pins the split against the full action vocabulary, so a newly added action has to be placed on one side deliberately rather than defaulting to unpaced. Separately, the interval is still counted in actions rather than requests, and one click was measured at fourteen of them -- so the floor is about an order of magnitude looser than its name suggests, which is recorded rather than fixed here.
- `SourcePace` bound its default path as a default argument, so it was fixed at import and redirecting `DEFAULT_PACE_PATH` redirected nothing. The test added with the pacing fix believed it was writing to a temporary directory and was in fact reading and overwriting this repository's own `data/browser_source_pace.json`, which made the suite depend on live pacing state and corrupt it: run twice in a row it failed the second time, because the first run had left a timestamp newer than the times the second one reported. The path is resolved on construction, and a test pins that redirecting it works.

- The two-key acknowledgement that lets a guarded source be reached never reached the browser channel, so the feature could not do the thing it was added for. `source_registry.build_source_plan` honoured `risk_acknowledged_sources` and admitted the source to the plan, and then `discovery_plan.py` -- which had never read `risk_accepted_sources` at all -- dropped it again on `automation_allowed is not True`. That test can never pass for a guarded source, because `source_registry` refuses a seed that claims both `requires_risk_ack: true` and `automation_allowed: true`; the acknowledgement was therefore inert by construction, and the only visible symptom was a source that cleared `risk_not_acknowledged` and then reappeared under `excluded.browser_policy`. Found by acknowledging both guarded sources on 2026-09-25 and watching the plan build four browser tasks, none of them theirs. The channel now honours an acknowledgement, and only an acknowledgement the catalog agrees with: being named locally is not enough, the seed must carry `requires_risk_ack` too, so a stale local name cannot open a source nobody gated. The `public_read_only_page` requirement is not waived by an acknowledgement -- `jobs-ie` stays refused for a reason the acknowledgement has nothing to say about. Each browser task now states its own `requires_risk_ack` alongside the source's `constraints`, so whoever executes a wave can see which tasks are there on an override rather than by default. The catalog check itself is unreachable through the public path, since seed validation forbids the shape that would exercise it; a test pins that invariant instead, and fails first if it is ever loosened.

- Browser pacing judged an action by when it was reported rather than by when it happened, so a properly paced run was refused and an impatient one would have passed. Caught in a live round on 2026-09-25: two actions genuinely 5.2s apart at the browser were reported 0.11s apart, and the second was written as `browser_paced_too_fast`. The reverse is the worse half -- a caller that hammered a site and reported slowly satisfied the gate -- and neither needs bad faith, only an Agent that performs a sequence and reports it afterwards, which is the normal shape of this work. `record_action` now takes `occurred_at` (`--occurred-at-ms` on the CLI, defaulting to now so every existing caller keeps its meaning) and the gap is measured between actions. A report arriving out of order yields a negative gap, which measures nothing and is refused rather than quietly accepted; the stored time never moves backwards.

### Added

- A source whose operator forbids automated access can now be reached, but only behind two keys that cannot both live in this repository. LinkedIn's robots.txt ends `User-agent: * / Disallow: /` and its User Agreement forbids "browser plugins and add-ons or any other technology" used to scrape the Services; Indeed's terms name "bots, scrapers, spiders, AI or Agentic AI" outright and disallow `/viewjob`, so a posting's own page is off limits even where the result list is not. Both are recorded in `source_seeds.json` as `automation_allowed: false` with `requires_risk_ack: true` and a `constraints` list naming why, and a test refuses a guarded source that states no reason. Such a source is planned only when its `source_id` also appears in `risk_acknowledged_sources` in the person's own `data/browser_provider.json` -- a directory this repository ignores, so a clone plans none of them and no commit here can open one in someone else's checkout. The plan reports `risk_accepted_sources` for the wave to show before it runs, and `excluded.risk_not_acknowledged` for the ones it left out. The flag is an override of a refusal and nothing else: declaring it on a source that permits automation is rejected, and the existing rule that `automation_allowed: false` cannot declare a direct access method still holds everywhere the flag is absent. The four standing prohibitions are outside the exception and unchanged -- no CAPTCHA bypass, no simulated login, no identity or IP masking, no detection evasion -- which is also what the operators' own crawling terms forbid, and what separates reading an authorised session from circumventing a control. These two sources cannot be verified the way every other entry is, because each of this repository's verification methods fetches the source and fetching is the thing they forbid; they carry `operator_terms_reviewed`, which is the only check that was actually performed.
- Browser actions are now paced per source, for every source rather than only the guarded ones. `browser_min_source_interval_ms` (default 5000, raisable but floored -- a limit that could be set to zero would be a suggestion) spaces two actions against one `source_id`; `browser_control.py pace --source-id S` reports what is left to wait, and `action --source-id S` enforces it. Nothing in this process can actually hold the Agent back, since it drives the browser through its own runtime, so an action that arrives too soon is still written -- staying visible -- but as `failure_kind=browser_paced_too_fast`, which keeps it out of the round's completeness. Pacing is per source because it is courtesy to the site being read, and two sites are not each other's traffic; an action reported without a `source_id` keeps exactly the meaning it had before. `browser_jitter_ms` (default 2000) adds a random wait on top of that interval and never takes one off it, so the enforced floor stays deterministic and the advised wait is always sufficient. It exists to spread requests out for the site being read, not to disguise them: bot detection reads TLS and browser fingerprints rather than the spacing between two page loads, and no part of this tries to defeat a classifier -- the prohibition on detection evasion is unchanged.
- A Web Search page can now report that it could not be timed, instead of being forced to invent a latency. `pages` required `duration_ms` on every page, and the executor it required it from is the Agent, which runs Web Search through a tool call it has no clock around: the tool returns titles and URLs and no timing, and bracketing the call with two shell timestamps measures the turn, not the search -- 11.6s on 2026-09-24 for a search that takes one to three. That left two exits, both dishonest: put a plausible number in the store, or report the whole task `skipped` and throw away candidates it really did find. A page may now declare `"timing": "unavailable"` and set `duration_ms` and `first_result_ms` to an explicit `null`; they go absent together, because half a stopwatch is not a measurement, and the key must still be present, because an omission reads the same whether it was meant or forgotten. The default is `"measured"`, so every page written before this keeps meaning what it meant. Two things stop it becoming a way out of the instrumentation it was added to protect: **the counts do not soften with it** -- `raw_results` and the rest are things the Agent can actually count, so the funnel and the per-task sums are still enforced -- and **the declaration is recorded rather than absorbed**, on the event as `timing`, on the batch result as `search_pages_untimed`, and in the rollup as `search.duration_ms.reported_rate`, which is what stops a round that timed nothing from looking exactly like a round that searched nothing, both being a null p50. Nothing here can tell an executor that cannot measure from one that did not bother; the point is only that it is no longer *made* to choose between two lies. `duration_ms` is nullable for `search` alone -- everywhere else the caller times its own work, and a null would only mean it forgot, which must stay indistinguishable from absence rather than become a legal value. The path #43 built was verified end to end for the first time on 2026-09-24: two real Dublin searches and eleven ATS boards committed as one wave, 13 of 13 tasks succeeded, 1,920 jobs fetched, 7 candidates merged, both pages recorded as untimed, and `reported_rate` fell to 0.6 across the store -- three older `search` events carry latencies because they came from the separate `search_metrics.py` call this contract replaced, and those are the events that used to be missing entirely.
- The city catalog lost postings that named a city without its country. `references/markets.json` listed Berlin, Munich, Hamburg, Frankfurt and Cologne but not Dusseldorf, Stuttgart or Hanover, and a city it does not list resolves to no market at all -- silently, because an unmatched location is indistinguishable from one in a market nobody targets. It cost a whole board: trivago posts every job in Dusseldorf, so the 2026-09-23 catalog build rejected it as having no target-market work. Measured across 6,532 real postings, only three were lost this way (`Hannover` twice, `Dusseldorf` once) because almost every posting names its country too and resolves at country level regardless; the ones carrying a bare city name are exactly the ones a gap drops. Six German and three UK cities were added, which also lifts 70 `Bristol, UK` postings from country to city precision, and trivago joins the ATS catalog as its 49th board. Cambridge, Birmingham and Oxford were deliberately left out: no market here models the United States, so nothing would contest a UK alias and `Cambridge, MA` would resolve as Cambridge, England -- the same confident wrong answer in the other direction. A test pins that they stay unresolved, and another pins that no city name is claimed by two cities. Region names are shared on purpose -- `England` names both London and Manchester -- so the new cities declare no administrative areas rather than make `Nordrhein-Westfalen` mean Cologne or Dusseldorf by catalog order.
- Remote job support was removed rather than repaired, and a round no longer searches remote work at all. `references/markets.json` carried a `remote_scopes` catalog that `normalize_location` read a location label against, and a qualifier the catalog did not list fell through to `global` -- worldwide. `Remote - US`, `Remote - India`, `Remote - Spain` and a bare `Remote` therefore all resolved identically, to the one answer that is wrong for three of them: the module could not tell "open anywhere" from "open somewhere else", while claiming it could, in a function whose own docstring is `without guessing unknown places`. Extending the catalog does not fix it. Of 422 real remote-labelled postings sampled on 2026-09-23, 51% state their hiring restriction only in the description -- `must reside in, and perform all work exclusively from within, the United States`, `based in Colorado, Hawaii, Illinois, Maryland`, `based in the UK, Ireland, Germany, the Netherlands` -- at a granularity finer than country, because the binding constraint is where an employer has an entity, an EOR or the appetite for permanent-establishment risk, not where the company sits. The deciding text lives in the job description, which by design never reaches the orchestrator, so searching remote honestly means changing the data flow rather than adding aliases. `open_to_remote` is gone from the CV profile and the prefilter, the scope catalog and its resolution are gone from `market_plan`, and a location naming remote is skipped whatever else it names. `location_normalized.remote_scope` stays in the CandidateEnvelope, always null: the contract and the 24 stored jobs keep their shape, and nothing claims a jurisdiction any more. `docs/roadmap.md` is new and records what doing this properly would require and how it would be judged done.
- The ATS board catalog went from 18 boards to 48, and Ireland -- the only market rolled out -- from 11 to 22. The structured channel is the only one producing jobs, so the pool it draws from was the ceiling on the whole 2026-09-23 run: 20 jobs reached the report and one of them scored an `apply`. 155 candidate tokens were probed by the existing rule -- no token trusted because it looks right, each fetched through this repository's own read-only adapter, markets taken from the locations a board's own jobs report rather than from where the company is headquartered -- and the 18 existing boards were revalidated first, which is what proves the probe works. 40 probes reached a live board and 30 were kept; 115 tokens had no board behind them, which is the expected cost of guessing one and spending one request to find out. Greenhouse went 14 to 30, Ashby 4 to 17, and **Lever 0 to 1** -- small but disproportionate, because the Lever adapter has shipped and been benchmarked since phase 1 without ever having a board in the catalog to fetch, so a seeded Lever board must now declare which of the two API hosts it lives on. Two rejections are catalog gaps rather than verdicts and are recorded as such: `trivago` posts only in Dusseldorf, which `markets.json` does not list although it lists Munich, Hamburg, Cologne and Frankfurt; `form3` reports `100% Remote (UK)`, and the normalizer rightly refuses to guess geography from a work-model string. `ats_boards_per_round` moved from 30 to 60 because the largest market now seeds 44 boards -- one request fetches one board, measured on a 694-job board, so 48 boards stay well inside the unchanged 100-request round budget. Several tests pinned the catalog's exact size and one picked its subject by sort order; they now pin the invariants -- every market lists exactly the seeds naming it, no two seeds claim one board, every board names a provider with an adapter -- so the catalog can grow without editing assertions.
- Every CLI here reads stdin to end-of-file, and end-of-file arrives only when the writer closes the pipe. Started from a background shell that keeps its end open, a script waits on that read forever: no output, no progress, no timeout, and nothing to tell it apart from slow work. `ats_pipeline.py run` did exactly that on 2026-09-23 and was left running for nine hours before anyone doubted it, which is the expensive half of the failure -- the hang itself cost minutes, not knowing cost the day. `_stdio.read_stdin_text()` bounds the wait at 30 seconds (`JOB_MATCHER_STDIN_TIMEOUT`, 0 to restore the unbounded read) and every one of the eighteen scripts that read stdin now goes through it, reporting `{"ok": false, "error": ...}` on stdout like any other input failure rather than a traceback a caller cannot parse. A terminal is exempt: end-of-file is one keystroke away there and the person can interrupt. Tests pin the failure itself against a real pipe whose writer never closes, and a per-script scan refuses a direct `sys.stdin.read()` so a new entry point cannot reintroduce it.
- `market_plan.normalize_location()` no longer lets a city lose to a country whose name happens to be longer. The winning match was chosen by alias length before specificity, so which reading survived depended on spelling rather than geography: "Frankfurt, Germany" kept its city, "Dublin, Ireland" did not. Every city whose name is shorter than its country's was downgraded to a country-level match -- Dublin, Cork, Berlin and Manchester among them, and "Dublin, Ireland" is the most common location string in this skill's own target market. `market_ids` was also plural in name and singular in fact: a posting open in "Dublin, Ireland; London, England" resolved to the United Kingdom alone, so a wave scoped to Ireland refused a plainly Dublin job, which is what blocked the first live structured wave. Specificity now decides first, then the earliest place named, then alias length; every matched market is reported, except that a shorter alias matched entirely inside a longer one is that longer name rather than a second place, so "Northern Ireland" does not also name the Republic.
- A discovery wave's structured tasks are now executed by `discovery_batch.py` itself instead of being reported to it. The channel that produces almost all of the jobs had no working path through the single writer: it emits a shape the CandidateEnvelope contract rejects outright, and the contract forbids the job description text those jobs carry, so its jobs could only reach the table through `ats_handoff.py` -- a second writer, outside the batch, its own merge, its own evaluation snapshot. Every existing batch test planned an empty `structured` list, so nothing noticed. The 2026-09-23 live run did: 20 real Dublin jobs fetched and uncommittable. `ats_pipeline.to_candidate_envelopes()` now emits contract-clean envelopes with the description text carried beside them rather than inside, `sync_registry()` takes `board_ids` so a wave fetches only the boards it planned and returns per-board rows so each task's outcome is measured, and the batch refuses a structured task result from its caller outright -- narration cannot stand in for a fetch the script performs itself. One wave still means one merge; the text reaches the merge subprocess and nothing else, and a replay does not touch the boards again. `merge_jobs.py` validates an envelope strictly and refuses unknown fields, so a structured candidate previously had to choose between being a valid envelope and keeping the description it was fetched with; the text is now lifted out before validation and put back after, reaching the run-scoped evaluation snapshot through the handoff that already existed for it. The carve-out is those two named fields -- any other unknown field is still refused.
- Every script already decoded stdin explicitly as UTF-8; nothing did the symmetric thing for stdout, which was left to the platform default. `scripts/_stdio.py` pins stdout and stderr to UTF-8, and the eight scripts that print unescaped JSON call it. The 2026-09-23 live ATS sync fetched 18 boards and 2643 jobs in 7.3s, wrote the registry and the sync state, and then died on `print()` with `UnicodeEncodeError: 'gbk' codec can't encode character ' '` -- one non-breaking space in one job title. The caller saw exit 1 and no candidates, the fetched jobs were discarded, and since the registry had already been updated the retry was skipped as recently fetched. A test pins the failure itself rather than a mutation of it, and the wiring is checked per script so a new one cannot forget.
- A verified ATS board is now asked for jobs on a job-freshness cadence instead of the registry's re-verification cadence. `ats_registry_ttl_days` gated both questions, so after one successful sync every board reported `boards_attempted: 0` for thirty days -- the cheapest discovery channel went silent and said only that it had attempted nothing. The 2026-09-23 live run hit exactly this: 18 boards synced the previous evening, zero fetched, zero candidates. A verified board is now due again after `ats_fetch_interval_minutes` (default 60, 0 for every round); the TTL keeps doing what it is named for, backing off from an `unavailable` board. Because every verified board is now due every round, the per-round cap serves the least recently fetched board first rather than the head of the list. The summary gained `boards_skipped_not_due` and `boards_skipped_by_cap`, so a round that fetched nothing is no longer indistinguishable from a round where nobody was hiring.
- `board_harvest.py` now recovers a board from a company's own careers URL, where the board token is not present to be extracted. An embedded ATS board puts only the provider and a job id in the URL (`gh_jid` / `ashby_jid`), so `extract_board_hint()` guesses candidate tokens from the hostname and the observed job id is what settles it: a guessed board is registered only when that job id is actually among the jobs it returns. A board answering is not evidence -- a hostname guess can land on a different company's board on the same ATS, which happened once during the catalog build. Guesses cost one request each and are bounded by their own `--hint-limit` (default 3), separate from the direct-board `--limit`. The job id is used for that check and discarded; only provider, token, and counts reach the registry.
- Web Search page counts now travel with the candidates they describe. A succeeded `web_search` task result carries `pages` -- one entry per result page -- and `discovery_batch.py` validates them and emits the `search` events itself. Previously the counts reached the metrics store only if the Agent remembered a separate `search_metrics.py` call after every page, and forgetting cost nothing at commit time: the candidates landed in the table regardless and the round merely closed `metrics_status=incomplete, missing_operations=search`, which is exactly what happened in the 2026-09-22 run. A Web Search task can no longer commit its candidates without them. The per-page grain is kept rather than collapsed into one batch-level event, and the page counts must both satisfy the filtering funnel and sum to the task's own `candidates_raw`/`candidates_prefiltered`, so they cannot be bookkeeping. Replaying a batch does not record its pages twice. `search_metrics.py` remains for Web Search that is not committed through a discovery batch.
- The cookie consent classifier now takes `container.visible` and returns `proceed` for a dialog that is not displayed. Sites routinely leave an empty, invisible `role=dialog` shell in the DOM after consent was already given; classifying that shell found zero safe actions and paused the run over a banner no human could see. Visibility only decides whether to stop -- a displayed shell still pauses, `ask_every_time` still wins, and no control is ever selected because of it.
- The ATS round budget raised in config exceeded `ats_pipeline`'s own ceiling, so the entire structured channel failed closed with `ats_boards_per_round must be between 1 and 10` before reaching a single board. The ceilings now sit at 30 boards and 100 requests, and a new config contract test asserts the shipped `config.json` is accepted by every script that reads it, plus that one market's board list fits inside a single round.
- A careers portal that redirects to its own public ATS board is now a board handoff instead of a dead end. Browser tasks carry `ats_handoff_hosts` and `on_ats_handoff`, `extract_board()` recognises board root pages as well as job URLs, and the redirect target goes straight to `board_harvest.py` -- so the employer is fetched in one structured request next round rather than browsed expensively or skipped as `host_boundary`. The 2026-09-22 trial lost a public-sector source exactly this way.
- `browser_control.py action`: a provider-neutral path for a local browser to report what the Agent executed. BrowserOS Neo and an authorized user browser are driven by the Agent's own tools, so this process never saw their actions and every Neo-backed run finished with `metrics_status=incomplete`, `missing_operations=browser`, and a report health of `unknown`. Reported actions now satisfy the browser completeness gate; the path needs no credentials or session budget, and only successful actions count.
- `seed_promotion.py`: harvested boards accumulate in `data/source_registry.json`, which is gitignored along with the rest of `data/`, so the growth never left one machine. Promotion appends verified, unexpired, URL-rebuildable sources to `references/source_seeds.json`, keeps `markets.json` in step, and re-origins the local record to `seed` so the next `merge_seeds()` does not reject its own catalog.
- `board_harvest.py`: an Ashby/Greenhouse/Lever job URL already carries its company's board identity, so one observed candidate now teaches the registry an entire employer. Each recovered board is probed once through the existing read-only adapter before it is trusted, its markets come from the locations seen in that probe, and only provider, token, markets, and counts are written -- never the URL, job title, JD, or CV.
- A verified public ATS board catalog: 18 Ashby/Greenhouse boards covering Ireland, the UK, and Germany, each confirmed by a read-only board fetch whose observed job locations determined its markets. Counts and rejection reasons are in [docs/ats-source-catalog.md](docs/ats-source-catalog.md).
- Deterministic cross-channel discovery waves: the plan owns remaining-task availability, the Agent executes only the current wave, and `discovery_batch.py` exposes a next wave only after canonical merge yield checks pass.
- Count-only live evidence for a BrowserOS Neo plus Web Search first wave, including semantic platform search, consent pauses, local attention state, open-web contribution, and fail-closed suppression of the next wave.
- Count-only evidence for a completed three-wave BrowserOS Neo plus Web Search run: 12 terminal task results, two explicit skips, three single-writer batch commits, one suppressed duplicate, two unique strong-identity candidates, and a `plan_exhausted` stop in an automatically cleaned temporary store.
- Count-only evidence for a full real-CV production run through three discovery waves, canonical merge, two JD evaluations, and a two-job HTML report, including explicit site compatibility outcomes and the current local-browser runtime-metric gap.
- A fail-closed, multilingual accessibility classifier and HTML setting for `necessary_only` versus `ask_every_time`; it never reads Cookie storage or selects accept-all/granular consent controls.
- Traditional-Chinese and generic English exact-label coverage for necessary-only consent, added from live consent banners without introducing site-specific selectors.

- A read-only `DiscoveryPlan` compiler that joins localized market queries, the URL-free source-health plan, and the public source catalog into bounded browser, Web Search, and optional structured-source tasks without hard-coded site selectors.
- Diversity-first browser source selection, source-policy enforcement, public Web source hints, and explicit semantic-accessibility/auth/privacy constraints for each browser task.
- A restart-safe `discovery_batch.py` handoff that requires one terminal result per current-wave task, validates CandidateEnvelope provenance against its task, commits all wave channels through one canonical merge, retries source-state failures without repeating merge, and returns a count-only continuation decision.
- Read-only source-batch preview validation so malformed source proposals/events fail before candidate persistence.
- A read-only discovery-mode selector with explicit `coverage`/`auto`/`model_only`/`browser_only`/`combined` routing, single-route Neo-first legacy `auto` fallback, and deterministic connection-loss versus human-action behavior.
- A provider-neutral local-browser capability probe and runtime adapter contract for BrowserOS Neo or an authorized user browser, with dedicated-tab privacy rules and canonical CandidateEnvelope handoff through the existing merge writer.
- A loopback-only local-browser control panel with persisted non-sensitive discovery mode, polling status UI, flashing browser-tab/beacon alerts, CSRF-protected resume requests, and a low-cardinality event CLI for login, verification, consent, and rate-limit pauses.
- Count-only Phase 4 runtime evidence for successful BrowserOS Neo MCP and authorized Chrome fallback paths, plus a real LinkedIn login pause/panel-resume/authenticated-session/same-tab-search handoff; the full single-market CV production path is now verified, while pagination, broad site compatibility, CAPTCHA/ambiguous-consent recovery, rate-limit recovery, and local-browser metric completeness remain live gates.
- Versioned `ie`/`uk`/`cn`/`de` market metadata, English/German/Simplified-Chinese role taxonomy, and a deterministic opt-in market planner with city/country/remote-scope normalization.
- Synthetic ten-observation-per-market offline fixtures and fixed strong-identity/routing ground truth for the four initial markets.
- A validated public source seed catalog with three local entry points and ten global company portals per initial market.
- A PII-safe, single-writer source registry with deterministic eligibility planning, TTL revalidation, idempotent proposal/event batches, and atomic legacy ATS import/rollback.
- A strict JSON/Python `CandidateEnvelope` contract and an opt-in dual-route handoff that serializes regional-registry and Agent Web Search candidates through the existing canonical merge writer.
- Batch-idempotent canonical merges, additive legacy provenance normalization, route/source/market discovery metrics, and retry manifests for merge-before-registry interruption recovery.
- Offline Phase D1 report evidence: market/source/verification filters, per-observation provenance, multi-source badges, market coverage cards, and explicit failed/skipped/not-collected/unknown empty states.
- An explicit Phase D2 four-market public-source smoke harness with fixed request/body/timeout/redirect limits, policy-enforced China skip behavior, count-only evidence, and a bounded 2026-09-18 run artifact.
- A strict count-only Phase E shadow-run contract, idempotent atomic evidence ledger, live-evidence-aware per-market rollout gate, and explicit off/shadow/opt-in/default market configuration that leaves every market off by default.
- A read-only Phase E shadow comparator that joins shared strong identities in memory, attributes regional/Web incremental contribution deterministically, computes JD/link and potential Top-N counts, and emits no candidate identities or business content.

### Changed

- `docs/multi-region-implementation-todo.md` no longer reads as if nothing had been built. Its 233 checkboxes are acceptance criteria written at kickoff and never ticked after delivery, so the document implied that every phase was outstanding when Phase 0/A/B/C/D/F all shipped in v2.4.0. It now opens by saying it is a specification rather than a progress board, points at `CHANGELOG.md` and `shadow_gate.py status` for authoritative state, and carries a per-phase reconciliation with the evidence for each. Phase E is the one genuinely undelivered phase: the ledger holds 2 shadow runs against a threshold of 3 per market across 2 dates, leaving uk one successful run short, ie one run plus a conclusive live smoke, and cn and de two runs each on separate dates. Three tests now hold the document to `config.json`, so the status cannot silently expire again.
- Discovery budget now follows measured cost: the structured and Web Search channels own wave 1, while the browser is deferred by the new `browser_first_wave` (default 2) and narrowed to two sources per market per wave. The existing wave gate then withholds browser tasks entirely when the cheap channels already produced enough. `browser_first_wave: 1` restores the previous all-channels-at-once behaviour.
- `ats_boards_per_round` rises from 10 to 30 and `ats_requests_per_round` from 30 to 100; a measured board costs roughly one request and a fraction of a second, so the old caps bound the cheapest channel far tighter than its cost warranted.
- Source proposals accept `board_token` and `instance`, under the same rules as seeded boards: an `ats_board` proposal without a token, or one naming a provider with no adapter, is rejected.
- The repository default is now `ats_enabled: true`. The public ATS path is the cheapest discovery channel measured so far (one request per board, roughly 0.2-0.6s, job descriptions included) and stays bounded by its own request/page/concurrency caps.
- Structured discovery tasks now carry `provider`, `board_token`, and Lever's `instance`, so a board is fetched by provider identity instead of its human-facing `entry_url`.
- The repository default discovery mode is now `coverage`: model/Web Search runs alongside the preferred available browser provider (BrowserOS Neo, then an authorized user browser). Legacy `auto` retains its single-route compatibility behavior.
- Report language and market search languages are separate in the new planning boundary; `zh` and `zh-CN` normalize to internal `zh-Hans`, while the existing single-region Web Search path remains available.
- `markets.json` now references stable Phase B source IDs, while `multi_region_enabled: false` continues to prevent the new source plan from changing the production discovery path.
- Once the generic registry is initialized, the existing ATS pipeline reads and writes its ATS-board view there; without it, the legacy `ats_companies.json` path remains the compatibility fallback.
- Runtime metrics schema v6 adds low-cardinality Phase C discovery counts and merge provenance/idempotency fields without recording queries, titles, companies, URLs, CVs, or JDs.
- Legacy candidate arrays remain accepted by `merge_jobs.py`; strict discovery validation is activated by the Phase C `discovery_route` boundary, while `candidate_handoff.py` always validates the full envelope.
- Browser-discovered candidates reuse the canonical table and now preserve `browseros_neo` or `user_browser` provenance in the bilingual HTML report.
- Candidate provenance distinguishes `global_job_board` from market-local job boards, with bilingual report labels and scoped-read guidance that excludes account navigation metadata.
- A count-only live candidate smoke validates one scoped BrowserOS Neo extraction and active detail page through CandidateEnvelope and the real merge writer in an automatically cleaned temporary store; the initial unscoped-read privacy regression is retained as failed evidence.
- A bounded three-candidate same-page BrowserOS Neo batch preserves strong identities, browser route, and global-job-board provenance 3/3 through the temporary merge while keeping uncertain detail pages at `unknown`.
- Multi-region run metadata is additive: old `{profile_summary,new_count,cached_count,lang}` files still render, while new metadata can provide target markets, search/report languages, run time, and count-only route summaries.

### Fixed

- A job on a company's own careers page now shares its identity with the same job on the ATS vendor's host. A company that embeds its board serves the posting from its own domain with the provider job id in a query parameter and no vendor hostname anywhere in the URL, so `stripe.com/...?gh_jid=123` and `boards.greenhouse.io/stripe/jobs/123` were two records and two evaluations whenever the two listings phrased the title differently. `gh_jid` and `ashby_jid` now yield `greenhouse:<id>` and `ashby:<uuid>`; both parameter names are vendor-specific and their value formats match each provider's own job ids, so neither needs the host check `jk` requires. Only conventions that exist are recognised -- GitHub code search finds `gh_jid` 51712 times and `ashby_jid` 948, against 6 and 1 for `lever_jid` and `smartrecruiters_jid`. Stored records adopt the key on the next merge through `_ensure_job_identity()`, keeping their `record_id` so cached evaluations survive.
- Two different jobs on one company careers page no longer collapse into one record. `canonicalize_url()` keeps a short allowlist of job-id query parameters and drops the rest, so a careers page holding its job id in any other parameter -- `ashby_jid`, among others -- gives every job on that page the same host+path `url_key`. Both merge paths treated that key as an identity and merged on it alone, which did not produce a duplicate: the second job was absorbed into the first and lost, title and URL and all, in the same batch or against a record stored in an earlier round. A weak `url_key` now has to agree on company and title as well; a provider job id is still an identity on its own. Refusals are reported as `weak_url_key_collisions_prevented`.
- Every local store now shares one exclusive file lock (`scripts/_filelock.py`) instead of five hand-rolled near-copies with three defects between them. Windows reports `PermissionError`, not `FileExistsError`, while a holder's `unlink()` is still pending, and `exists()` reports `False` in that same window: measured over a 3-second, 5-thread handoff, 711 of them. `merge_jobs`, `source_registry`, and `runtime_metrics` read that pair as proof the lock was unreachable and failed the caller outright -- an aborted merge, an aborted registry write, a metric dropped with nothing to show for it; `shadow_gate` and `browser_control` did not catch `PermissionError` at all and let it escape. A real access failure is now told apart from a handoff by outlasting the timeout rather than by one observation, and the deadline is checked on every retry path, including the two that previously retried without consulting it.
- The lock's 50 ms poll capped handoff throughput at ~20/s however short the critical section was, so twenty contending writers spent 970 ms of a 2 s budget asleep and `test_twenty_concurrent_writers_preserve_parseable_table` flaked about once per full-suite run. At a 5 ms poll the same twenty writers finish in 0.17 s with a worst wait of 163 ms, taking the margin against the timeout from 2x to 12x.
- `shadow_gate`'s ledger lock had no stale handling at all, so a lock left behind by a killed process wedged the ledger until someone deleted the file by hand. It now reclaims a lock older than `STALE_LOCK_SECONDS`, matching every other store.
- `board_harvest.py` validated a custom seed catalog against the default `markets.json` rather than the one it was given, so a promoted catalog would fail its own consistency check on the next harvest.
- Seeded ATS boards reach the pipeline with their identity intact: seed-to-registry conversion dropped `board_token`, so every `ats_board` seed became a tokenless record that `ats_view_from_registry()` silently skipped, leaving the structured channel empty no matter how the catalog was curated.
- Seed validation now rejects an `ats_board` without a `board_token`, and rejects `ats_public_api` access for a provider that has no adapter, instead of planning a structured task that can never be fetched.
- Teamtailor job URLs now produce provider-owned `teamtailor:<job-id>` strong identities, allowing valid Web Search candidates to pass the shared CandidateEnvelope and canonical deduplication boundary.
- Phase E no longer treats missing real-CV Top-N baselines or repeated zero-useful-candidate runs as default-enable evidence; v2 count-only shadow records carry explicit baseline status while historical v1 ledger hashes remain readable.
- The legacy-table migration regression now uses a fixed clock, preventing its fixture from becoming stale as the real 30-day archive TTL advances.

## [2.4.0] - 2026-08-30

Detailed release notes: [docs/releases/v2.4.0.md](docs/releases/v2.4.0.md).

### Added

- A non-blocking startup version check that compares the local semantic version and Git revision with GitHub `main`, caches public metadata for 24 hours, and never auto-updates local files.
- Runtime metrics schema v5 with one PII-safe pipeline `run_id`, `run_start`/`run_finish` lifecycle events, Web Search page metrics, optional subagent token/cost fields, and explicit completeness reporting.
- A run-scoped metrics contract that pre-registers privacy rules and A/B quality/cost/latency gates before any further optimization claim.
- A bounded, count-only interleaved ATS HTTP-compression A/B harness with content-equivalence, request-count, job-count, and minimum wire-reduction gates.
- A bounded three-provider ATS quality collector and count-only audit with pre-registered JD coverage, false-positive, direct/adjacent calibration, contract, and browser-fallback gates.
- ATS-to-evaluation JD handoff: normalized Ashby/Greenhouse/Lever descriptions now flow through run-scoped task snapshots so eligible workers can skip a second page fetch.
- `ats_handoff.py`, an orchestration entry point that keeps full ATS candidates in process/subprocess stdin while returning only count summaries and merge task metadata to the agent.
- Count-only JD availability, truncation, handoff, and handoff-character metrics, plus a reproducible three-provider Fake handoff benchmark.
- Opt-in production ATS enhancement pipeline for public Ashby, Greenhouse, and global/EU Lever boards, with a persistent board registry, deterministic CV-aware prefilter, independent request/page/concurrency budgets, partial-success handling, and merge-ready strong identities.
- Shared production/Fake ATS Provider contract and offline regressions covering single-response boards, sequential Lever pagination, EU routing, unlisted Ashby jobs, global request exhaustion, 404/429/timeouts, disabled routing, and unavailable-board transitions.
- PII-safe ATS sync state and runtime schema v4 metrics, plus a fixed three-provider Fake benchmark integrated into the release performance harness.
- A PII-safe controlled Web-only versus Web+ATS discovery-to-merge benchmark with isolated canonical tables, fixed local inputs, production hard-cap enforcement, and Web-result preservation checks.
- ATS Phase 1 architecture, official public-API contract notes, a bounded six-board benchmark, and PII-safe raw/summary evidence for Ashby, Greenhouse, and Lever.
- Regression tests for provider normalization, Ashby unlisted filtering, Lever sequential pagination/caps, failure metrics, output privacy, and weak identity collision reporting.
- Stable `record_id` and Provider-owned `identity_keys` for the canonical job table and evaluation snapshots, with lazy in-place migration for legacy tables.
- PII-safe identity migration/conflict metrics in runtime schema v3 and a fixed cold-core performance comparison.

### Changed

- Runtime health is now `unknown` rather than `healthy` when a finished run is missing required events or an unfinished run exceeds the configured age limit; unavailable token/cost values remain `null` instead of being counted as zero.
- Public ATS requests now advertise gzip support by default while independently bounding compressed wire data and decompressed JSON; the transport switch remains injectable for controlled A/B tests.
- ATS HTML descriptions are converted to plain text, script/style content is discarded, and each handoff is capped at 50,000 characters. Evaluation workers treat it as untrusted data and use browser fetching only when no ATS text is available.
- Canonical jobs persist only `jd_content_hash`; a changed hash invalidates cached JD analysis and match scores. Completed/conflicted tasks immediately discard transient JD text and completed/expired run snapshots are deleted.
- The public ATS benchmark now reuses the production parser and pagination implementation so benchmark contracts cannot drift from runtime behavior; ATS remains explicitly disabled by default.
- Greenhouse discovery now recognizes EU public job-board pages while continuing to use the documented global API endpoint; oversized `content=true` responses may retry once without content inside the existing request budget, with response bytes and fallback counts recorded.
- Exact Provider IDs and canonical URLs now match before weak company/title matching. Disjoint strong IDs never merge by weak key alone; weak-only matches require compatible locations and exactly one target.
- Evaluation workers now echo `record_id`; legacy results without it remain accepted only when their `dedup_key` identifies exactly one task and job.

### Fixed

- Runtime metadata now reuses the Python 3.10-compatible version reader instead of importing Python 3.11-only `tomllib`, restoring the declared Python 3.10 CI path.
- Greenhouse entity-escaped HTML is decoded before plain-text extraction, preventing literal tags from entering evaluation input; Lever normalization now includes documented `lists` and `additionalPlain` sections instead of dropping requirements and closing content.
- ATS title prefiltering no longer accepts mobile, Android, iOS, or UI roles solely because an `AI` product suffix overlaps a preferred AI role. Standalone `ai` is low-information while explicit `AI evaluation`, `AI systems`, and `agent systems` phrases remain eligible.

### Security

- Compressed ATS payloads are decompressed through a bounded stream, preventing a small wire response from bypassing the existing 25 MB response limit.
- Raw ATS JD text is excluded from the canonical table, command output, runtime metrics, sync state, benchmark reports, and evaluation history; it exists only in an active local evaluation task.
- The ATS benchmark only performs allowlisted public GET requests and never persists job descriptions, titles, URLs, candidate data, API keys, or arbitrary exception text.
- The controlled ATS A/B artifact is count-only and omits CV/profile fields, board tokens, company names, job titles, URLs, API keys, and raw exception text.

### Performance

- A three-pair live A/B across one Ashby, Greenhouse, and Lever board kept 292 jobs, 292 JD-bearing jobs, and 3 requests identical in every arm. Median wire bytes fell from 4,581,392 to 948,090 (-79.31%); median wall time moved from 2,407.832 ms to 2,278.456 ms (-5.37%). Content fingerprints matched in all pairs. The byte reduction is the accepted optimization; public-network variance means the latency observation is not a guaranteed speedup.
- The Phase 5 bounded live sample used 3 public GETs across Ashby, Greenhouse, and Lever, normalized 291 jobs in 2,672.85 ms, and handed off complete JD text for 7/7 sampled jobs. All 7 five-dimensional results passed the contract; one direct job scored 92.0, five adjacent jobs averaged 65.15, one false positive produced a 14.29% false-positive rate, and no adjacent job was inflated to `strong_apply`. This is a small 3/3/1 provider sample with only one direct job, not a market-wide precision or browser A/B claim.
- The 30-iteration Phase 5 offline regression recorded core total p50/p95 of 53.541/60.395 ms and ATS Fake normalization p50/p95 of 5.133/5.536 ms. Both were slower than the Phase 4 snapshot, alongside slowdowns in unchanged core stages, so no performance improvement or parser-specific regression is claimed.
- The Phase 4 offline handoff baseline completed three-provider ATS normalization at p50 4.094 ms / p95 5.108 ms and normalization-through-evaluation-snapshot handoff at p50 7.408 ms / p95 8.367 ms across 30 runs. Each run handed off 3/3 JDs, made all 3 tasks eligible to skip page fetching, and persisted zero raw JDs in the canonical table. No live latency or scoring-quality claim is made from this fixture.
- A one-board live validation handed off JD text for 8/8 emitted candidates with one request; a purposive three-JD five-dimensional audit produced 2 `strong_apply` and 1 `apply`, with 3/3 contract updates accepted, zero conflicts/rejections, and zero raw JD text in completed tasks, the canonical table, or metrics. The sample is single-company and not a market-wide quality estimate.
- The three-provider offline ATS fixture completed 3 boards / 3 requests / 3 emitted jobs per iteration at p50 6.654 ms and p95 7.867 ms across 30 measured runs, with no external calls. The same machine run showed core total +22.6% p50 / +32.9% p95 versus the earlier checkpoint alongside similar slowdowns in unrelated harness sections; the observation is retained, causation is not claimed, and no core speedup is claimed.
- The production-adapter public regression completed 6/6 boards with 7 requests, 414 normalized jobs, no truncation/rate limiting, and zero strong-identity duplicates; its artifact is count-only and PII-safe.
- The controlled Phase 3 run preserved all 5 fixed Web records and produced 24 combined unique records: 19 incremental identities and 1 avoided duplicate evaluation. Three boards succeeded with 5 requests and 48,955,686 response bytes in 12,653.682 ms end-to-end. No candidate-quality or browser-fallback improvement is claimed because JD evaluation handoff was not measured.
- The initial title-level quality audit failed: the fixed Web control was 4/5 target-relevant, while ATS output was 5/20 target-relevant, 3/20 adjacent/stretch, and 12/20 false positives. After the filter fix, the final bounded replay emitted 8 candidates: 6 target-relevant, 2 adjacent/stretch, and no false positives (75% strict precision). All 8 links were alive, all 5 Web records were preserved, and 1 duplicate evaluation was still avoided; single-company concentration remains a coverage warning.

## [2.3.0] - 2026-08-25

Detailed release notes: [docs/releases/v2.3.0.md](docs/releases/v2.3.0.md).

### Added

- Optional Kernel BYOK remote-browser fallback with visual screenshot/mouse/keyboard controls, bounded listing pagination, Live View handoff, and a deterministic Fake Provider for CI.
- A one-shot setup page bound to `127.0.0.1`; provider keys are tested before being stored in the OS keychain, with `KERNEL_API_KEY` as the non-UI fallback.
- Atomic per-round admission limits: at most 2 concurrent browsers, 3 pages per site, 10 sessions, 10 minutes of handoff wait, and USD 1.00 estimated cost by default.
- Role-specific subagent profiles and PII-safe requested/effective model, reasoning-effort, latency, success, valid-item, and fallback metrics.
- PII-safe browser action/session/handoff/rate-limit/estimated-cost metrics and runtime schema v2, with v1 event compatibility.
- `scripts/benchmark_pipeline.py` for repeatable 15-job cold-core and 10-session Fake Provider release measurements, including raw runs and baseline deltas.
- `[project]` metadata in `pyproject.toml` with `version` as the single source of truth, read at runtime by `_jobutil.skill_version()` and reported by `summarize_metrics.py`.
- Tests that fail when `pyproject.toml`, the newest `CHANGELOG.md` release heading, and `docs/releases/vX.Y.Z.md` drift apart.
- Documentation drift tests: every script and every `config.json` knob must appear in both READMEs, every release note must be linked, and no knob may exist that nothing reads.

### Documentation

- `WORKFLOW.md` now distinguishes web-search result pagination from sequential job-site listing pagination and specifies model selection, metric recording, remote-browser fallback, budget admission, handoff, and cleanup.
- The confirmed architecture and staged ATS boundary are recorded in `docs/browser-provider-control-panel.md`; ATS integration remains a later independently validated phase.
- Both READMEs now cover `round_timer.py` and `cp_hash.py`, the `eval_run_stale_hours` and `consecutive_empty_stop` knobs, the multi-market search strategy, batch overlap, abandoned-snapshot recovery, and the untrusted-input boundary.
- `search_playbook.md` names `stop_threshold`, `max_websearch_calls`, and `consecutive_empty_stop` instead of hardcoding their values in prose.
- The fallback ladder in `WORKFLOW.md` and `scoring_rubric.md` now honors `enable_headless_fallback`, which previously existed in `config.json` but was referenced nowhere.
- `docs/releases/v2.2.0.md` records the post-release controlled measurement of batch overlap (measured 16.6–22.1% saving, within 0.3 pp of the model) and narrows the remaining limitation to live-latency variance.

### Removed

- `cv_cache` and `report_keep_history` from `config.json`. Neither was read by any script or referenced by any instruction document — CV profiles are always cached and reports always keep history — so they promised control that did not exist.

### Security

- API keys, cookies, session IDs, Live View URLs, page URLs, typed text, screenshots, CV/JD text, and arbitrary exception strings are excluded from metrics by strict field and category allowlists.
- Remote stealth is disabled by default; the workflow prohibits automatic CAPTCHA solving, login simulation, proxy rotation, or bypassing site controls.

### Fixed

- Real Kernel SDK 0.94.0 smoke testing found that `type_text` no longer accepts the legacy `smooth` keyword. The adapter now uses the current `type_text(id, text=...)` contract, and the optional dependency range records the tested `0.94.x` API family.
- The same smoke run found that the remote Linux host expects the X11 `Return` key symbol instead of the common agent spelling `Enter`. The adapter now normalizes `Enter`, `ENTER`, and combinations such as `Ctrl+Enter` before calling Kernel.

### Performance

- On the fixed 15-job cold benchmark, core total p50 changed from 55.741 ms to 60.666 ms (+8.8%) and p95 from 64.107 ms to 64.604 ms (+0.8%), with all 15 jobs merged, updated, and rendered in every run. This feature release does not claim a deterministic-core speedup; the small render overhead remains visible and documented.
- The new Fake Provider path completed 10 sessions / 70 recorded actions per iteration at p50 56.763 ms and p95 73.022 ms with 100% success. No external requests or provider charges were used.

## [2.2.0] - 2026-08-08

Detailed release notes: [docs/releases/v2.2.0.md](docs/releases/v2.2.0.md).

### Added

- `scripts/round_timer.py` and a PII-safe `round` metric event (`round_duration_ms`, `orchestration`, `batches`, `evaluations`, `jobs_reported`) that time a full matching round, making the serial-vs-overlapped comparison measurable instead of merely modeled. Round durations are excluded from script-level `duration_ms` percentiles; `summarize_metrics.py` reports per-mode p50/p95 and `overlap_saving_pct`.
- Overlapped orchestration guidance in `WORKFLOW.md` and `SKILL.md`: batch N evaluation workers and batch N+1 search workers are spawned in the same message, backed by the existing eval-run snapshot/conflict machinery; `max_parallel_subagents` is documented as a shared global budget (1 search + 2 evaluation during overlap).
- One-stop precise-ranking worker guidance: fetch JD, extract `jd_profile`, and score inside a single subagent, keeping the full JD text out of the orchestrator context.
- Regional job-platform URL canonicalization (liepin, zhipin, lagou, seek, reed) so cross-source dedup gets exact `url_key` hits outside the international ATS ecosystem.
- Multi-market site strategy in `search_playbook.md` (Ireland/UK, continental Europe, Australia/NZ, mainland China, plus a locale-inference rule for other markets).
- Query-variant dedup rule in `search_playbook.md`: seniority/stack modifiers on the same role no longer spend extra websearch budget.
- LLM fallback rule for closed-posting detection in `scoring_rubric.md`; `_CLOSED_PATTERNS` is now grouped per language for easy extension.

### Changed

- Recommendation thresholds now match JobRadar: `stretch_apply` ≥ 60 (was 55) and `low_priority` ≥ 20 (was 40).
- `scoring_rubric.md` adds deterministic seniority caps (ported from JobRadar's profile guards) and clarifies that hard-filter/deal-breaker hits lower `overall_score` by lowering the affected dimension scores, never by editing the weighted total directly.
- `analysis_contract.py` rejects recommendations more aggressive than the score band (downgrades stay allowed), so an evaluation can no longer pair a low score with `apply`.
- `_CLOSED_PATTERN` covers JobRadar's newer evergreen-posting phrases ("this exact role may not be open", "posting is to advertise potential job opportunities").

### Fixed

- Stale evaluation runs (pending longer than `eval_run_stale_hours`, default 2) and corrupt run manifests are now abandoned during `merge`, releasing jobs that would otherwise stay `in_evaluation` forever; abandoned runs are logged to `data/eval_runs/history.jsonl` and counted as `abandoned_runs` in merge stats.
- Reports no longer fall back to match scores from a different CV/candidate-profile pair; such jobs render unscored with a "needs re-score" badge instead of showing a misleading score.
- `_aggregate_batch` no longer drops a second URL from the same source within one batch (listing page + detail page); extra URLs are kept as `alt_urls` and feed `all_url_keys` for exact matching.
- `_aggregate_batch` now falls back to `url_key` matching after `dedup_key`, so an aggregator re-listing with a rewritten title (same job id in the URL) is collapsed before evaluation is dispatched instead of after, removing one wasted LLM evaluation per occurrence.

### Security

- Escape `</` when embedding job/meta/health JSON into the HTML report so external job content cannot break out of the inline `<script>` block.
- Reject non-http(s) job and source URLs (for example `javascript:`) before they reach report links.
- Prompt-injection guidance in `references/scoring_rubric.md` and `references/search_playbook.md`: search results and JD text are untrusted data; embedded instructions must be ignored.

### Removed

- `scripts/_build_table.py`, a leftover one-off script that bypassed the merge contract and carried hardcoded personal data.

## [2.1.0] - 2026-07-31

Detailed release notes: [docs/releases/v2.1.0.md](docs/releases/v2.1.0.md).

### Added

- PII-safe `data/metrics.jsonl` events for merge/update success and failure paths.
- Runtime duration and lock-wait p50/p95/p99, evaluation rates, queue backlog, and configurable health thresholds.
- `scripts/summarize_metrics.py` for Markdown/JSON health reports and automation-friendly threshold exits.
- Automatic 7-day and 30-day health snapshots embedded in every generated HTML report.
- A header health indicator and full-screen, bilingual monitoring view with KPI cards and threshold alerts.
- Deterministic tests for metric sanitization, summary calculations, failure recording, and concurrent JSONL appends.
- Render integration tests for window isolation, no-data/degraded states, graceful monitoring failure, and PII exclusion.

### Fixed

- `stats.new` now means jobs added by the current merge instead of all rows still carrying `status: new`.

## [2.0.0] - 2026-07-31

Detailed release notes and migration guidance: [docs/releases/v2.0.0.md](docs/releases/v2.0.0.md).

### Added

- Run-scoped, minimal evaluation snapshots under `data/eval_runs/`.
- Evaluation result schema validation, including score ranges and weighted-total checks.
- Cross-process table locking, atomic JSON replacement, record versions, and evaluation-input hashes.
- Conflict detection, safe rebasing, partial commits, idempotent retries, and automatic run cleanup.
- Eight deterministic tests for concurrency, stale results, validation, corruption handling, and lifecycle behavior.
- GitHub Actions CI on Python 3.10 for Ubuntu and Windows.

### Changed

- Search and evaluation computation may overlap, while all canonical-table commits remain serialized.
- `data/jobs_table.json` remains the only canonical job table; evaluation workers no longer write shared state directly.
- `merge` now returns an `eval_run` manifest containing the run ID and task path.
- `update` now requires `--run-id` and accepts only evaluation-owned fields associated with that run.
- Corrupt canonical JSON now fails closed instead of being treated as an empty table.

### Security

- Evaluation manifests exclude the raw CV and avoid copying the complete canonical job table.
- Search-owned fields cannot be overwritten by evaluation output.
- Out-of-range scores and malformed evaluation results are rejected before persistence.

### Breaking

- Direct callers of `scripts/merge_jobs.py update` must pass the `run_id` returned by the corresponding `merge` call.
- Evaluation workers must echo `dedup_key`, `base_record_version`, and `jd_input_hash` from their assigned task.
- Legacy evaluation results without run-scoped snapshot metadata are rejected.

### Verification

- Local: 8/8 tests passed in 1.38 seconds; Ruff and Python compilation passed.
- GitHub Actions: 8/8 tests passed on Ubuntu in 2.12 seconds and Windows in 0.95 seconds.
- Main CI run: [30623328782](https://github.com/sangowu/job-matcher-skill/actions/runs/30623328782).

[Unreleased]: https://github.com/sangowu/job-matcher-skill/compare/v2.2.0...HEAD
[2.2.0]: https://github.com/sangowu/job-matcher-skill/compare/v2.1.0...v2.2.0
[2.1.0]: https://github.com/sangowu/job-matcher-skill/compare/v2.0.0...v2.1.0
[2.0.0]: https://github.com/sangowu/job-matcher-skill/compare/v1.0.0...aefbdf9816a0ff17f246eb3c4b501cffa3e51c25
